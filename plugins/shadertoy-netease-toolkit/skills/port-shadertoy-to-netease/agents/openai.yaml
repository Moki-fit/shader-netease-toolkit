interface:
  display_name: "Port Shadertoy to NetEase"
  short_description: "Analyze, review, or port Shadertoy GLSL into NetEase"
  default_prompt: "Use $port-shadertoy-to-netease to check the local Shadertoy library first, then analyze, review, or port the confirmed source through the correct NetEase Minecraft render route."

policy:
  allow_implicit_invocation: true
